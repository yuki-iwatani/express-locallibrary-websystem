extends layout

block content
  h1(style="color: #800080")= title
  body(style="background-color: 	#e6e6fa")

  if author_list.length
    ul
      each author in author_list
        li
          a(href=author.url) #{author.name}
          |  (#{author.lifespan})

  else
    p There are no authors.
